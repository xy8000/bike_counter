# Root-level ignore. Each sub-project (backend/, frontend/) ships its own
# .dockerignore; this file only guards against stray build artifacts if a
# Dockerfile is ever built from the repository root.
target/
node_modules/
.git/
