# Rust build artifacts
target/
debug/

# Version control
.git/
.gitignore

# Local configuration / secrets (all configuration lives in config.toml)

# Compose / docs (not needed for the image build)
docker-compose.yml
Dockerfile
README.md
ToDo.md
plans/
