# 66 - Merge the frontend (nginx) into the backend image (single image)

Status: dropped — abandoned after architecture discussion. The two-image split is
kept; nginx remains in the frontend as the static server and reverse proxy.
